services:
  - type: static
    name: manwon-generator
    buildCommand: "echo 'Static site build'"
    staticPublishPath: .
    pullRequestPreviewsEnabled: false
    headers:
      - path: /*
        name: Cache-Control
        value: max-age=0